#!/usr/bin/env python3
"""Run the source checkout without installing any dependencies."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "src"))
from agent_session_relay.cli import main

raise SystemExit(main())
